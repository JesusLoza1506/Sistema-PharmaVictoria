# Configuración SMTP para envío de correos
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.auth=true
mail.smtp.starttls.enable=true
mail.smtp.user=lozayataco@gmail.com
mail.smtp.password=bahx gnoo ejji jgeh
# =============================================================================
# CONFIGURACIÓN DE BASE DE DATOS PHARMAVICTORIA
# =============================================================================

# Configuración MySQL
db.url=jdbc:mysql://localhost:3307/pharmavictoria?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver

# HikariCP Pool Configuración
db.pool.maximum-pool-size=3
db.pool.minimum-idle=1
db.pool.connection-timeout=30000
db.pool.idle-timeout=600000
db.pool.max-lifetime=1800000
db.pool.connection-test-query=SELECT 1

# Configuración de la Aplicación
app.name=PHARMAVICTORIA
app.version=1.0.0
app.empresa.ruc=20123456789
app.empresa.razon-social=PHARMAVICTORIA E.I.R.L.

# Configuración de Seguridad
security.session.timeout=28800
security.password.min-length=8
security.max-login-attempts=3

# Configuración de Logging
logging.level.root=INFO
logging.level.com.farmaciavictoria=DEBUG
logging.file.name=pharmavictoria.log

# Configuración de Sistema de Puntos
puntos.por.sol=1
puntos.por.descuento=100
descuento.por.puntos=5.00

# Configuración de Ventas
venta.descuento.maximo.vendedor=5.0
venta.descuento.maximo.admin=100.0

# Configuración de Alertas
alerta.stock.minimo=10
alerta.vencimiento.dias=30